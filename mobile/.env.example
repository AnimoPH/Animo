# --- Pick ONE backend ---
# Local full stack (listings + model): after `npx supabase start`,
#   npx supabase status -o env
# Use API_URL and ANON_KEY (eyJ… JWT). Not PUBLISHABLE_KEY.
# EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# EXPO_PUBLIC_SUPABASE_ANON_KEY=
#
# Hosted team project (shared data; will NOT use Docker on your laptop):
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Dev login credentials (must exist in the project above — local Auth is empty)
EXPO_PUBLIC_DEV_FARMER_EMAIL=
EXPO_PUBLIC_DEV_FARMER_PASSWORD=
EXPO_PUBLIC_DEV_BUYER_EMAIL=
EXPO_PUBLIC_DEV_BUYER_PASSWORD=

# Alchemy config
ALCHEMY_API_KEY=
POLYGON_RPC_URL=
