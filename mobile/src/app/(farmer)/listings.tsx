import { Sprout } from 'lucide-react-native';

import { PlaceholderScreen } from '@/components/animo/placeholder-screen';

/** Aking Ani — farmer's own listings. Scaffolded; buyer module is priority. */
export default function FarmerListingsScreen() {
  return (
    <PlaceholderScreen
      title="Aking Ani"
      icon={Sprout}
      message="Dito ninyo malilista at mapapamahalaan ang inyong ani. Inihahanda pa namin ang bahaging ito."
    />
  );
}
